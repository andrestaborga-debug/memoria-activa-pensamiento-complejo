{
  "name": "penales-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute penales-db --local --file=./src/schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
